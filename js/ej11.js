// 11. Crea un programa que solicite al usuario ingresar un número y determine si es divisible entre 3.

let num = prompt('Introduce un número:');

if (num%3==0) {
    console.log('El número '+num+' es divisible entre 3.');
} else console.log('El número '+num+' no es divisible entre 3.');
